# Server-only credential used by the Phase 2 provider feasibility spike.
LIVE_TENNIS_API_KEY=
# Opt-in server-side MLB Stats API schedule/linescore integration. No key is required.
MLB_API_ENABLED=false
# Opt-in server-side Premier League integration via football-data.org.
FOOTBALL_DATA_ENABLED=false
FOOTBALL_DATA_API_TOKEN=
